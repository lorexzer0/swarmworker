node_modules/
dist/
*.log

# build / tooling artifacts
web/dist/
server/dist/
.vite/

# playwright-mcp verification artifacts
.playwright-mcp/
*.png

# local data (also lives in ~/.swarmworker)
.swarmworker/

# local env overrides (.env.example is committed as a reference)
.env
.env.local
.env.*.local
